@model StrygevaleDatabase.Models.Subject

@{
    ViewBag.Title = "Подробно";
}

<h2>Подробно</h2>

<div>
    <h4>Предметы</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayName("Название предмета")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.subject_name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Редактировать", "Edit", new { id = Model.id_subject }) |
    @Html.ActionLink("Назад", "Index")
</p>
