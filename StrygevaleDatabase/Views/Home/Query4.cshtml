@model IEnumerable<StrygevaleDatabase.Models.Student>

<p>
    @Html.ActionLink("Создать новый", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayName("ФИО")
        </th>
        <th>
            @Html.DisplayName("Название класса")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.fio, "Details", new { id = item.id_student })
        </td>
        <td>
            @Html.ActionLink(item.Class.class_name, "Details", new { id = item.id_student })
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_student }) |
    
            @Html.ActionLink("Delete", "Delete", new { id=item.id_student })
        </td>
    </tr>
}

</table>
